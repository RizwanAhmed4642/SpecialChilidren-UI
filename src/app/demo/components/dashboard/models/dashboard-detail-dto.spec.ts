import { DashboardDetailDto } from './dashboard-detail-dto';

describe('DashboardDetailDto', () => {
  it('should create an instance', () => {
    expect(new DashboardDetailDto()).toBeTruthy();
  });
});

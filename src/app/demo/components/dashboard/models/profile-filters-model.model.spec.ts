import { ProfileFiltersModel } from './profile-filters-model.model';

describe('ProfileFiltersModel', () => {
  it('should create an instance', () => {
    expect(new ProfileFiltersModel()).toBeTruthy();
  });
});
